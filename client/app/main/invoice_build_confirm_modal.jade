div.modal-header
	h3.modal-title 產生發票號碼
div.modal-body
	div.alert.alert-danger 請再次確認日期是否正確: {{info.dt | date : 'yyyy-MM-dd'}}
	div.input-group
		input.form-control(type='password', placeholder='請輸入密碼')
		span.input-group-btn
			button.btn.btn-danger(type='button', ng-click="ok()", ng-disabled="!enableCreateInvoice") 確認
	div.jumbotron {{ret_data}}
	button.btn.btn-primary(type='button', ng-click="exportAction()") pdf
	table.export-table.table.table-hover.table-bordered
		thead
			tr
				th order_id
				th invoice_no
				th date
		tbody
			tr(ng-repeat="invoice in ret_data.success")
				td {{invoice.order_id}}
				td {{invoice.invoice_prefix + invoice.invoice_no}}
				td {{invoice.date_added | date: "yy-MM-dd": "+0800"}}

div.modal-footer
	button.btn.btn-primary.pull-left(type='button', ng-click="cancel()") 取消
	button.btn.btn-primary.pull-right(type='button', ng-disabled="enableCreateInvoice", ng-click="printInvoice()") 列印統一發票